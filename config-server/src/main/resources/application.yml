server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sunildevops45/E-Commerce-Microservice.git
          default-label: master
          searchPaths:
            - configuration
            - configuration/user-service
            - configuration/order-service
            - configuration/product-service
